# deploy/.env.example

# Docker agent image to run. If building locally from your DeepSeek source, set IMAGE_NAME and use docker build.
# If you want to use a prebuilt image, set AGENT_IMAGE instead.
# Example prebuilt: ghcr.io/deepseek-ai/agent:latest

# Use local build (true) or prebuilt image (false)
USE_LOCAL_BUILD=true

# Local image name to build from DeepSeek source
IMAGE_NAME=mozart/deepseek:local

# If using prebuilt image, set this
AGENT_IMAGE=ghcr.io/deepseek-ai/agent:latest

# Public domain for the agent UI (must point to this VM's public IP)
DOMAIN=agent.example.com

# Local internal port agent serves on inside the container
AGENT_PORT=8000

# Optional: set to 'true' to enable GPU (requires NVIDIA drivers present on host)
USE_GPU=false

# Optional agent environment variables (example)
AGENT_API_KEY=changeme
AGENT_WORKDIR=/data/deepseek

# Caddy data path (TLS storage)
CADDY_DATA_DIR=/var/lib/caddy
